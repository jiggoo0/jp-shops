/* @identity เจ้าป่า */
export interface FlightInfo {
  id: string;
  from: string;
  to: string;
  departure: string;
  arrival: string;
  price: number;
}

export interface KBTConfig {
  themeColor: string;
  logoText: string;
}
